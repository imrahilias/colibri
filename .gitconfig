# only one global config possible, dont state mail globally; get prompted if
# local config does not exist!

# Gitlab
#[user]
#name = Siegel, Moritz
#email = moritz.siegel@tuwien.ac.at

# Github
#[user]
#user = Imrahilias
#email = siegel.moritz@gmail.com

[filter "lfs"]
process = git-lfs filter-process
required = true
clean = git-lfs clean -- %f
smudge = git-lfs smudge -- %f

#thats from 1passwords ssh agent.
#[gpg]
# format = ssh
# [gpg "ssh"]
# program = /opt/1Password/op-ssh-sign
#[commit]
#gpgsign = true
