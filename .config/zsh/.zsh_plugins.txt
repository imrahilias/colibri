# antidote uses this to compile zsh plugins

#        |            _)
#  __ \  | |   |  _` | | __ \   __|
#  |   | | |   | (   | | |   |\__ \
#  .__/ _|\__,_|\__, |_|_|  _|____/
# _|            |___/

# set up Zsh completions with plugins
mattmc3/ez-compinit kind:defer
zsh-users/zsh-completions kind:fpath path:src kind:defer

# some errors, seems the path_completion is broken?
#chitoku-k/fzf-zsh-completions kind:defer

# always fzf for everything is maybe not what i want.
#lincheney/fzf-tab-completion path:zsh

# rga-fzf can already do that on its own.
#ppcamp/zsh-fzf-rg kind:zsh

# auto suggestion:
zsh-users/zsh-autosuggestions kind:defer

# fzf replaced that.
#zsh-users/zsh-history-substring-search kind:defer

# add own functions to antidote easily.
#mattmc3/zfunctions

# syntax highlighning has to be last:
#zsh-users/zsh-syntax-highlighting kind:defer
zdharma-continuum/fast-syntax-highlighting kind:defer
